version: '3.8'
services:
  flask-app:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - bento-service
    restart: always

  bento-service:
    build: .
    ports:
      - "3000:3000"
    restart: always
